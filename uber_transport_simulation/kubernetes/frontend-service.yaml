apiVersion: v1
kind: Service
metadata:
  name: frontend-service
spec:
  selector:
    app: frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80 # Adjust to match Django container's port
  type: ClusterIP # Or NodePort if you need it exposed externally\

